# Cloudflare Web Analytics token. Get from Cloudflare dashboard:
# Analytics & Logs -> Web Analytics -> your site -> token.
# Copy to .env (and to Cloudflare Pages env var or GitHub Actions secret).
PUBLIC_CF_ANALYTICS_TOKEN=
